# Analysis dependencies for poc_stage4 analysis scripts (Phases 1-3+)
# Tested in conda environment poc_stage2 (Python 3.12.13)
# Install via: conda run -n poc_stage2 pip install <package>

numpy>=2.0
scipy>=1.11       # false_discovery_control added in 1.11; BH correction for Phase 3
matplotlib>=3.9
pandas>=2.0
