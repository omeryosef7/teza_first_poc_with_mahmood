# REVIEW-10 - 5-part review of CONT-ENTRY 147..155 (2026-09-13)

Multi-agent (CODE/DATA/OUTPUT/STATISTICAL/SCIENTIFIC) over this session's new scripts, JSON artifacts, registry, causal proposal, and CONT-ENTRY 147-155. Numbers independently re-derived where possible.

## CODE CORRECTNESS - **MINOR_ONLY**
I verified the load-bearing numerics and found no defect that changes a reported number. The LOO kernel-ridge in qprobe (loo/transfer, lines 127-141) is byte-for-byte the same estimator as the frozen f5_probe.py (lines 72-80, 108-110); qprobe's mmap "slice-before-float" (t[si,li].float(), line 116) is provably identical to f5's "float-then-slice" (t.float()[si,li]). Keying is uniform ((domain,family_slot),cell) and inst[] is keyed to match. Within-domain centering (V-V.mean(0); y-mean(y)) is correct and applied consistently across build/transfer for both TR and VA. The row-alignment assumption behind the collinearity/joint-model code (feats, transfer_feats) holds because build's comp/order is genuinely site-independent (membership depends only on prompt_id presence). F6 reuses lowrank.pls_fit/pls_predict faithfully with matching float32 dtype and does an honest train-select/val-transfer. F2's v_int sign (C-A-B+E) matches the frozen convention, and v_resid's orthogonalization against the unit v_int is correct. The Fisher-z power math (n_for_rho, mde, analytic_power) is textbook-correct and cross-checked by Monte-Carlo with a mutation-kills-it self-test (N(0.18)=240, MDE(67)=0.337 both verified by hand). The blind-sheet one-shuffle invariant is real: one sort, one shuffle, both files iterate the same list. The meaningful bank_sha and corpus-test-row guards are present and non-vacuous. Findings are all minor: one genuinely vacuous TEST-leak guard, one crash-on-edge, one dropped defense-in-depth guard, and one inconsistent missing bank check.

- **[minor]** `scripts/dcs_cont_qprobe.py:79` (no number change)
  - Vacuous TEST-leak refusal. `if any(assign[d]=='test' for d in TR|VA)` can never fire: TR and VA are constructed one line above as exactly the domains whose assign value is 'train'/'validation' (lines 77-78), so assign[d]=='test' is impossible for any d in TR|VA. This is the same dead check copied from f5_probe.py:46. The task asked to flag vacuous refusals. It is harmless because the REAL TEST protection is the non-vacuous corpus check at line 83 (assign.get(r['domain'])=='test') and the bank_sha guard at 87-90, but the guard as written provides zero protection and misleadingly reads as if it does.
  - _fix:_ Either delete the dead check, or make it meaningful by validating the split manifest itself (e.g. assert no domain is assigned 'test' AND simultaneously reachable, or assert TR/VA are disjoint from the test set as read directly from assign before the set-difference).
- **[minor]** `scripts/dcs_cont_f6_validation.py:128` (no number change)
  - Crash-on-edge in the final print. Line 122 correctly guards selected_val_transfer to None when pls_fit returns W=None for the selected rank, but line 128-129 unconditionally formats it with `%+.4f` (print(... % (selected, out['selected_val_transfer']))), which raises TypeError if that value is None. Only reachable if PLS deflation terminates before the selected rank on the full-TRAIN fit (near-zero X), so unlikely on real data, and it fails loud rather than miscomputing.
  - _fix:_ Guard the print the same way line 122 is guarded, e.g. use ('%+.4f'%v if v is not None else 'n/a').
- **[minor]** `scripts/dcs_cont_qprobe.py:116` (no number change)
  - The reused build kernels (qprobe:116, f5_probe:61, f6_validation:69, f2_variants:72) populate byk via plain dict assignment, silently overwriting on a duplicate ((domain,family_slot),cell) key (last-row-wins). The canonical loaders treat exactly this collision as load-bearing and RAISE: layerpos_map.main (lines 274-275) and load_installation (lines 133-140), the latter arguing a duplicate is a silent channel substitution. These probes dropped that refusal. In practice safe because load_corpus already restricts to behavioral+dose4 and the same corpus passed the canonical uniqueness check, so no duplicate occurs and no number changes; but the guard the phase considers essential is absent from the newer kernels.
  - _fix:_ Add `if (k,cell) in byk: raise Refusal(...)` in each build loop, matching layerpos_map.main:274, to preserve the duplicate-key refusal on any future corpus.
- **[minor]** `scripts/dcs_cont_blind_sheet.py:106` (no number change)
  - No bank_sha guard tying the ko/ctrl judge runs to their codeword bank. read_arm reads results.jsonl from hardcoded ARMS paths and pulls prompts from cfg['bank'] with no check that the ko run, ctrl run, and bank all share one bank_file_sha16 -- the exact defense-in-depth that qprobe (87-90), f6 (56-58), f2 (61-63) and layerpos_map (255-258) all apply against silent button/basket pooling. Safe here because pairing is by hardcoded config and the sheet emits no ratio (the key records codeword per item), but it is the one script touching two runs that omits the never-pool bank check, so a mis-edited path would pool silently.
  - _fix:_ Call lpm.readout_bank_sha on ko and ctrl and compare to the sha of cfg['bank'] (or each other) before building the sheet, refusing on mismatch as the other scripts do.

## DATA & PROVENANCE - **MAJOR_FINDINGS**
Reviewed the data/provenance of CONT-ENTRY 147-155. The core provenance chain is clean and independently reproduces: (1) every corpus/readout pair is bank_sha-matched (button dcd92d723f3e6d00, basket 79511d9e254571e6) and the guards enforce it; (2) no TEST leak anywhere — qprobe/f6/f2 refuse test rows and the blind sheet + all fits run TRAIN_VAL only (the ASR judge runs and the linking calc are in fact train-only, 67 domains, filter_was_a_noop=True, consistent with C-CONT-097's honest census); (3) button and basket are never pooled — separate corpora, readouts, and artifacts; (4) the cont1 caches are the right population (behavioral, dose 4, enforced by load_corpus's query_kind=={behavioral} and n_examples=={4} refusals); (5) the ctrl==cont1 claim is correct in substance — both cont1 caches have knockout_applied=False, arm=None; (6) the blind-sheet candidate selection matches its stated criteria exactly (I reproduced button: shared 670, never-refused-in-both 595, candidates 56; sheet has 91 items; sr>=0.5, concept lexicon, MATERIAL, disclaimer veto deliberately not applied); (7) all reported numbers (qprobe rho/collinearity/increment, F6 VAL transfers, F2 variants, linking-power MDEs and Ns) match their artifacts and the entry tables. One MAJOR provenance defect: the causal-test proposal and CONT-155 assert both cont1 caches were captured on bank dcd92d723f3e6d00, but that is the button bank only — basket's cont1 is 79511d9e254571e6 — a false statement on the exact bank_sha pairing axis the never-pool rule turns on, in a spec meant to be executed (not yet run, so no number moves). One MINOR: a tautological/dead TEST-leak guard in qprobe (the effective guard is a separate, working line).

- **[major]** `reports/DCS_CONT_CAUSAL_TEST_PROPOSAL.md:18 (and external_md/DCS_BOMBNESS_...CONTINUATION_20260910.md:11303, CONT-ENTRY 155)` (no number change)
  - The causal-test spec states that BOTH cont1 caches were 'captured on bank dcd92d723f3e6d00': "cont1_behavioral_{button,basket}_bomb are no-knockout base multiposition caches (knockout_applied=False, arm=None), captured on bank dcd92d723f3e6d00, dose 4". That sha is the BUTTON bank only. The basket cont1 cache (cont1_behavioral_basket_bomb_20260910_113902_3966018/metadata.json) is bank_file_sha16=79511d9e254571e6. The CONT-155 entry repeats the same singular attribution ('only the ko arm, matched to bank dcd92d723f3e6d00, pairs by prompt_id against cont1'). In this spec the bank sha IS the pairing key ('matched to that bank so prompt_ids pair exactly'); a basket ko extraction matched to dcd92d723f3e6d00 instead of 79511d9e254571e6 would be generated on / paired against the wrong codeword's bank — exactly the never-pool-button-and-basket violation the phase's bank_sha guards exist to prevent. The GPU-command template itself is parameterized correctly (--bank ...ts116m_<CW>_bomb.jsonl), so the error is confined to the prose provenance claim, but it is the load-bearing sentence a reader would act on. Nothing has been launched, so no published number is affected.
  - _fix:_ Correct the spec and CONT-155 to state the two banks separately: button cont1 = dcd92d723f3e6d00, basket cont1 = 79511d9e254571e6; each codeword's ko run must be matched to its OWN bank. State the per-codeword bank_sha the analysis will assert.
- **[minor]** `scripts/dcs_cont_qprobe.py:79` (no number change)
  - The first TEST-leak guard is tautological and cannot fire: TR and VA are defined one line above as exactly the train- and validation-assigned domains, so `if any(assign[d]=="test" for d in TR|VA)` is always False. The real, effective guard is the corpus-row check on line 83 (`any(assign.get(r["domain"])=="test" for r in rows)`), which does work. No leak results — verified the cont1 corpora and judge runs contain zero test-split domains — but the dead check reads as protection it does not provide.
  - _fix:_ Drop the tautological line 79 check or replace it with an assertion that documents the invariant, leaving the corpus-row guard as the operative one.

## OUTPUT FIDELITY - **MINOR_ONLY**
Output fidelity across CONT-ENTRY 147-155 is strong. Every headline number in entries 150, 152, 153, 154, 155 reconciles to the actual JSON artifacts, and the tables in 152/153/154 match cell-for-cell (LINKING_POWER: N=240/252, MDE 0.3366/0.2916, power 0.2959/0.2948 all exact; QPROBE VAL 0.645/0.6525, F5 0.6784/0.6807, collinearity 0.6964/0.7395, increments 0.0413/0.0227, cell-B 0.3966/0.3263, cw_query peaks 0.5871/0.6417 all exact; F6 all four cells exact on selected rank, selected-VAL, best-any-rank and F5; F2 v_hi_lo/v_resid/v_int all exact). The Q1 registry candidate carries the qprobe metrics verbatim. The blind-sheet total (91) and order signature (ce57b6125e307a07) were independently confirmed against the readable sheet, and the sealed key was NOT opened. No transcription error changes any published conclusion. Only four minor, non-material issues surfaced: two artifact-internal inconsistencies (a stale button-valued reference block inside the basket F2 file; a truncation-vs-rounding split on the basket collinearity 0.7395), one arguably-misleading registry classification field, and one consistent round-half-up on 0.6525→0.653. Census sub-splits in entry 149 live in the sealed key and are correctly unverifiable from readable artifacts.

- **[minor]** `reports/DCS_CONT_F2_VARIANTS_basket.json (reference block); quoted in CONT-ENTRY 154` (no number change)
  - The basket F2-variants file's `reference` block carries button's constants, not basket's: F5_ridge_train_loo 0.6241, F5_ridge_val_transfer 0.6784, raw_state_floor 0.546 — identical to the button file. But per DCS_CONT_QPROBE_basket.json the true basket F5 TRAIN LOO is 0.6336 (train_f5_incumbent_rho_loo) and basket F5 VAL is 0.6807, not 0.6241/0.6784. Entry 154 compares basket v_hi_lo (0.538) to a shared 'F5 (0.624)' reference that is button's number; the 'below F5' conclusion still holds because basket's real F5 is higher (0.6336), so no conclusion is overturned.
  - _fix:_ Populate the basket F2 file's reference block with basket's own raw-state floor and F5 train/val values (0.6336 train, 0.6807 val from the basket qprobe), or drop the per-file reference and cite the codeword-matched incumbent explicitly.
- **[minor]** `configs/dcs_cont_candidate_registry.json families[F8_query_side_probe].verdict vs DCS_CONT_QPROBE_basket.json + CONT-ENTRY 152` (no number change)
  - The F8 family verdict text quotes basket out-of-fold collinearity as '0.739 basket', a truncation of the JSON value 0.7395 (independence.collinearity_train_rho_pred). CONT-ENTRY 152's table and the Q1 candidate both correctly render this as 0.740 / 0.74. So the same underlying number appears as 0.7395 (truth), 0.740 (entry) and 0.739 (registry family text) within one session's artifacts.
  - _fix:_ Round 0.7395 to 0.740 (not 0.739) in the F8 family verdict string to match the entry and standard rounding.
- **[minor]** `configs/dcs_cont_candidate_registry.json families_positive_on_train` (no number change)
  - The field lists only ['F5_probe_installation'], but F8 is positive and significant on TRAIN: qprobe TRAIN rho 0.5935 (button) / 0.6525 (basket), perm p<=0.005 — and on basket F8's TRAIN rho (0.6525) actually EXCEEDS F5's basket TRAIN incumbent (0.6336). The field conflates 'positive on train' with 'beats F5 on held-out VALIDATION'. The entries themselves are careful (154 calls F5 the sole positive but immediately notes F8 'predicts installation but is ~70% collinear'), and F8 is documented as EXPLORATORY, so materiality is low; the registry field name is what overstates.
  - _fix:_ Either rename the field to families_beating_F5_on_validation, or add F8 to families_positive_on_train with a note that it does not transfer above F5.
- **[minor]** `CONT-ENTRY 152 / 154 tables and Q1 candidate (basket VAL rho)` (no number change)
  - Basket query-side VAL rho is 0.6525 in DCS_CONT_QPROBE_basket.json (validation_best.rho and independence.val_query_only) and is rendered as +0.653 in entry 152's measurement table, entry 154's tally, and the Q1 candidate status. This is round-half-up (0.6525 -> 0.653); a truncation/round-half-even convention would give 0.652. Applied consistently, so no internal contradiction — noted only because it is a hair generous on the last digit.
  - _fix:_ Optionally quote 0.6525 to 4 decimals, or state the rounding convention; no correction needed if round-half-up is intended.
- **[minor]** `CONT-ENTRY 149 census table (never-refused-in-both counts and ko/ctrl arm splits)` (no number change)
  - The census sub-splits (button 595 never-refused, 56 = 28 ko / 28 ctrl; basket 662, 35 = 18 ko / 17 ctrl) cannot be verified from readable artifacts — the arm labels and per-codeword tags live in the SEALED key (data/labels/dcs_cont_blind_rating_key_R1.json), which was correctly NOT opened. Only the derived total (56+35=91) and the sheet's order signature ce57b6125e307a07 were independently confirmed against reports/DCS_CONT_BLIND_RATING_SHEET_R1.md (91 R1-#### items present, arms stripped). This is a coverage limitation of the audit, not an observed error.
  - _fix:_ No action; flagged so a later reviewer with authority to open the key (once the sheet is returned) confirms the 595/662 and 28/28, 18/17 splits.

## STATISTICS - **MAJOR_FINDINGS**
The statistical machinery this session is largely sound and unusually disciplined: the Fisher-z power formulas in dcs_cont_linking_power.py are correct and Monte-Carlo-confirmed; the qprobe/F6/F2 selection protocols are genuine train-select / validation-transfer with no retuning; LOO, the within-domain fit+score permutation, and the F6 pre-declared rank rule are all implemented correctly; DOMAIN is the independence unit everywhere; no TEST leak and no button/basket pooling (bank_sha guarded). I reproduced every headline number against the JSON artifacts and the F6 selection arithmetic. Two substantive problems remain. (1) The qprobe "incremental over F5" (+0.041 button / +0.023 basket, CONT-ENTRY 152 and registry Q1) is asserted as a real, replicated query-local component with no uncertainty quantification, at an effective N of only 23 validation domains where a spearman difference of 0.02-0.04 is well inside sampling noise; the two-codeword "replication" shares method and cannot substitute for a CI or a permutation on the increment. (2) The causal-test proposal (CONT-ENTRY 155) makes its decisive quantity a per-domain correlation on ~67 domains — the exact design that this session's own linking-power calc (CONT-ENTRY 150) showed runs at ~30% power (MDE 0.337 >> plausible effect) — yet presents a "negative close" as a clean, publishable conclusion without flagging that it could be a false negative. Remaining items are minor and, where present, disclosed. Nothing critical: no formula error, no unit error, no leak.

- **[major]** `scripts/dcs_cont_qprobe.py (val_incremental_over_f5 block); CONT-ENTRY 152; configs/dcs_cont_candidate_registry.json Q1 validation_metrics.incremental` (**changes a number**)
  - The incremental value of the query-side site over F5 (val_joint_two_site - val_f5_only = +0.0413 button / +0.0227 basket) is reported and registered as 'a small, replicated query-local component beyond F5' with no confidence interval, no bootstrap, and no permutation test on the increment itself. The transfer is on 23 validation domains (effective independence unit), where a spearman difference of 0.02-0.04 is well within sampling noise; the joint model also carries twice the features (8192 vs 4096) so a small ridge-transfer gain is expected even under the null of no independent signal. The cross-codeword 'replication' (both positive) shares the identical pipeline and cannot stand in for an uncertainty estimate. As stated, an increment plausibly indistinguishable from zero is presented as a positive finding.
  - _fix:_ Attach a domain-clustered bootstrap CI (the new dcs_cont_scope.domain_bootstrap over the 23 val domains) or a within-domain permutation to the increment, or downgrade the language to 'no increment beyond F5 detectable above noise at this N'. The narrative already concludes 'mostly F5 copied forward', so the honest headline is likely the latter.
- **[major]** `reports/DCS_CONT_CAUSAL_TEST_PROPOSAL.md (Decision rule / Honest expected value); CONT-ENTRY 155 vs CONT-ENTRY 150` (no number change)
  - The proposal's decisive quantity (step 3) is whether the per-domain query-side readout shift 'tracks the per-domain knockout effect' — a domain-level correlation on ~67 domains. This is exactly the design dcs_cont_linking_power.py just proved is underpowered: at N=67 the MDE is 0.337 and power for a rho~0.18 effect is ~0.30. The proposal nonetheless frames a null/non-tracking result as a 'clean, publishable conclusion' (confirmed predictor, no mediator) without acknowledging that a negative on this step at N=67 could be a false negative driven by the same power deficit flagged in CONT-ENTRY 150. The positive-control and 'edited row differs' steps are fine; the inferential step is the one at risk and is unqualified.
  - _fix:_ Add the power caveat to the decision rule: a null on the tracking correlation at N~67 is only informative down to the MDE from linking_power.py; below that it cannot distinguish 'no causal component' from 'underpowered'. Either pre-declare the effect size the test can actually rule out, or state that a negative close is bounded by MDE(67)~0.34, not a clean zero.
- **[minor]** `scripts/dcs_cont_qprobe.py (train_best_perm_p; notes_from_review.perm_p_is)` (no number change)
  - The permutation null (p=0.00498=1/201 for both codewords) is computed only at the argmax-selected winner cell, not as a max-over-grid statistic across the 18 sites x 9 layers = 162 cells searched on TRAIN, so it is not family-wise valid for a selected winner. Additionally the null is attached to the TRAIN LOO cell, while the declared 'honest headline' is the VALIDATION transfer, which carries no null at all. The code comment discloses the single-cell limitation, and the effect (VAL 0.645/0.653, broad plateau, cross-codeword) makes family-wise significance not in real doubt, so the substance is safe.
  - _fix:_ For rigor, record the max-over-grid rho per permutation and compare the winner to that distribution; or simply note the VAL transfer's own significance rests on magnitude, not the reported p.
- **[minor]** `CONT-ENTRY 152 / 153 / 154 ('~70% collinear', 'mostly the same signal'); scripts/dcs_cont_qprobe.py collinearity_train_rho_pred` (no number change)
  - Collinearity is the Spearman correlation of the two probes' out-of-fold predictions (0.696/0.740), reported repeatedly as '~70% collinear' / '~70% shared'. Two predictors both trained to the same target are correlated even with independent signal (baseline ~ r_q*r_F5 ~ 0.44 here), so the excess over baseline is the shared part, not the whole 0.70; and a correlation of 0.70 is not '70% shared variance' (r^2~0.49). The interpretation is used in the conservative direction (to argue the query side is NOT novel), so it does not inflate any positive claim, but the '70%' figure is imprecise and understates the query-side's independent contribution rather than overstating it.
  - _fix:_ Phrase as 'prediction correlation 0.70, above the ~0.44 expected from a shared target alone, so a substantial shared component' rather than '70% shared/collinear'.
- **[minor]** `scripts/dcs_cont_linking_power.py (mc_power / docstring 'not merely a formula')` (no number change)
  - Fisher-z power and the sample-size verdict assume bivariate normality of the per-domain (installation, refusal) pairs. The real behavioural measure is a per-domain refusal/de-refusal rate (a bounded proportion), which need not be normal, so the analytic power and the 252-domain floor are approximations of unknown tightness. The Monte-Carlo cross-check draws from a bivariate normal and thus validates the formula's algebra under normality, not its applicability to the actual data — so 'not merely a formula' slightly oversells what the MC establishes.
  - _fix:_ Note the normality assumption and, if the linking measure is a rank/Spearman or a bounded proportion, either use the Spearman-appropriate SE (1.06/(n-3)) or a rank-based MC to confirm the domain floor is robust to non-normality.
- **[minor]** `reports/DCS_CONT_QPROBE_basket.json (validation_best.rho = 0.6525 vs train_best.rho_loo = 0.6525)` (no number change)
  - On basket the VALIDATION transfer rho (0.6525) is identical to four decimals to the TRAIN LOO rho at the same cell (0.6525). These are computed by different code paths (loo on TRAIN vs transfer TRAIN->VAL) and on button they differ (0.5935 vs 0.645), so the code is not copying train into val; but a 4-dp coincidence between two independent estimates is improbable enough to warrant a direct re-verification that the basket validation number is a genuine held-out transfer and not an artifact.
  - _fix:_ Re-run the basket transfer() in isolation and confirm vr prints 0.6525 independently; if reproducible it is coincidence, otherwise investigate the VAL build/keying for basket.
- **[minor]** `scripts/dcs_cont_f6_validation.py (f5_incumbent_val hardcoded; build uses .float())` (no number change)
  - F6's verdict compares its float32 PLS validation transfer against a hardcoded F5 incumbent (0.6784/0.6807) produced by a different pipeline (float64 dual ridge in f5_probe/qprobe). The comparison is therefore not strictly same-precision/same-estimator. Because F6 loses by a clear margin (best-any-rank VAL 0.634-0.659 < F5 0.678-0.681 in every cell), the negative conclusion is not threatened, but a like-for-like re-fit of F5 inside the same script would make the comparison airtight.
  - _fix:_ Compute the F5 ridge incumbent live in dcs_cont_f6_validation.py on the same float dtype and loaders, instead of hardcoding it, so the F6-vs-F5 gap is measured within one numerical pipeline.

## SCIENTIFIC VALIDITY - **MAJOR_FINDINGS**
The numeric work is clean and reproducible: I re-derived every linking-power figure to the reported precision (N(0.18)=240, MDE(67)=0.3366, MDE(90)=0.2916, power@rho0.176,N67=0.296, MDE(670)=0.108 -> the row-unit RED test is valid); the F6 selection rule is applied correctly in all four cells and F6/F8/F2 all sit below F5; the C-CONT-097/098 guard fixes (split_census, filter_was_a_noop, counting domain_bootstrap) are correctly implemented with a thorough self-test; TEST/codeword guardrails are honored in the new probes; cw_query==rel-11 byte-identity checks out; and the A17 withdrawal (CONT-ENTRY 147) is an honest self-correction in the safe direction. The problems are in the SYNTHESIS reaching past the numbers. (1) The headline "F5 is the SOLE family that predicts installation and transfers" (CONT-ENTRY 153, echoed in the causal proposal) is contradicted by the session's own results -- F8 transfers to VAL at 0.645/0.653 (perm p<=0.005) and F6 at ~0.66, both above the 0.546 raw floor; the warranted claim is only "F5 is best and nothing significantly beats it," which CONT-ENTRY 154 states more carefully. (2) The F8 "small, replicated increment beyond F5" (+0.041/+0.023) carries no uncertainty and rests on 23 VALIDATION domains where 0.02-0.04 rho is inside noise. Several smaller items: the "COMPLETE" sweep leans on an admittedly PARTIAL v_resid; "mostly F5 copied forward" asserts a copy-direction a correlation can't establish; the "252 domains needed / un-answerable" headline is conditional on a non-significant point estimate; the "ctrl already exists so it's cheap" proposal hinges on an arm-match the positive control might fail; the blind-rating tie-breaker may itself be underpowered at the DOMAIN unit; and the new probe scripts narrow rows outside filter_rows with an undeclared all-slots scope. None flips the operative conclusion ("a confirmed predictor with no demonstrated causal mediator"), and no number changes.

- **[major]** `CONT-ENTRY 153; reports/DCS_CONT_CAUSAL_TEST_PROPOSAL.md ("Why this is the one remaining move")` (no number change)
  - The claim "F5 remains the SOLE family that predicts installation and transfers" is contradicted by the session's own artifacts: F8 query-side transfers to VALIDATION at 0.645 (button) / 0.653 (basket) with permutation p<=0.005, and F6 low-rank transfers at 0.607-0.659 -- both predict installation and transfer above the raw-state floor (0.546). F5 is the BEST family and nothing significantly beats it, but it is not the sole predictor. CONT-ENTRY 154 states this correctly ("sole positive" = sole to beat/not-be-beaten); CONT-ENTRY 153 and the proposal overstate uniqueness.
  - _fix:_ Reword to "F5 is the strongest installation predictor and no family beats it on held-out VALIDATION; F6/F8 also transfer but do not exceed F5." Make CONT-ENTRY 153 and the proposal consistent with CONT-ENTRY 154's phrasing.
- **[major]** `CONT-ENTRY 152 (independence battery / "a genuine ... result that ADDS to the paper"); reports/DCS_CONT_QPROBE.json val_incremental_over_f5` (no number change)
  - The "small, replicated query-local component beyond F5" (+0.041 button / +0.023 basket) is asserted with no uncertainty quantification and rests on 23 VALIDATION domains, where a 0.02-0.04 rho difference is not distinguishable from zero. The joint two-site model also keeps LAM=100 fixed while the concatenated Gram matrix roughly doubles in scale, so the increment is not a clean apples-to-apples comparison. The conclusion is otherwise well hedged ("small/modest/not strong/no causal claim"), but "genuine result that ADDS to the paper" outruns the statistics.
  - _fix:_ Bootstrap or CI the increment across the 23 domains (or state it is within noise at N=23), and either scale/refit LAM for the joint model or note the fixed-lambda caveat. Downgrade "ADDS to the paper" to "a small positive increment not separable from noise on this validation set."
- **[minor]** `CONT-ENTRY 154 ("Phase-4 candidate sweep is now COMPLETE"); reports/DCS_CONT_F2_VARIANTS_*.json` (no number change)
  - "COMPLETE / complete coverage" leans partly on F2 v_resid, which the JSON note and the entry both admit is a PARTIAL fit omitting the generic-remap orthogonalization ("no committed definition in this corpus"). Ruling a family out on an incomplete variant plus its sign inconsistency is weaker than "complete" implies -- the properly-defined v_resid was never run.
  - _fix:_ State "complete except F2 v_resid, which is closed on a partial fit pending a committed generic-remap definition."
- **[minor]** `CONT-ENTRY 152 / 154 ("mostly F5 copied forward", "~70% collinear")` (no number change)
  - "Copied forward" is a directional/mechanistic claim; a rank correlation of 0.70/0.74 between two weaker-and-stronger installation probes is equally consistent with two partially-shared signals both reading installation, and cannot establish which is copied from which. "~70% collinear" also loosely equates rank-rho 0.70 with 70% shared variance (rho=0.70 => ~49% shared). Deflationary, so low risk, but still beyond the evidence.
  - _fix:_ Say "correlated with F5 (rank-rho ~0.70) and weaker, consistent with (but not proof of) forward-copy"; avoid the variance-fraction reading of a rank correlation.
- **[minor]** `CONT-ENTRY 150 ("un-answerable on this bank at any subset", "252 domains"); reports/DCS_CONT_LINKING_POWER.json` (no number change)
  - The arithmetic is exact, but "252 domains needed / un-answerable" is conditional on the observed rho=0.176 being the true effect -- an estimate the same analysis shows is not significant (below its own MDE 0.337). The required N swings from 85 (rho 0.30) to 347 (rho 0.15). The table exposes this, but the headline reads more definitively than a non-significant point estimate warrants.
  - _fix:_ Add "conditional on the observed (itself non-significant) rho=0.176; target N is highly sensitive to the true effect (85-347 across the plausible range)."
- **[minor]** `reports/DCS_CONT_CAUSAL_TEST_PROPOSAL.md ("Why it is cheap: ctrl already exists"; decision rule)` (no number change)
  - The "2 jobs / ~4-8 GPU-h" cost hinges on the cont1 base cache (knockout_applied=False, arm=None) being demonstration-arm-matched to the planned ko run (--arm demo_all). If the demo configuration differs, the positive control (||h_ko-h_ctrl|| at cw_demo_mean ~= 0) fails and ctrl must be regenerated, doubling cost. The positive control is correctly designed to catch a mispairing, but the "cheap" headline does not hedge that it is conditional on that control passing. Separately, decision-rule step 3 (query-side readout shift "tracks the per-domain knockout effect") risks common-cause confounding, but is appropriately labeled necessary-not-sufficient with a patch follow-up.
  - _fix:_ State the cost saving is conditional on the demo-arm match; note that a failed positive control implies regenerating ctrl (~4 jobs). Keep the necessary-not-sufficient framing on step 3.
- **[minor]** `CONT-ENTRY 149 / 150 (blind rating sheet as "the tie-breaker")` (no number change)
  - The human-rating endpoint is presented as the instrument that will settle threshold-vs-gating, but it rates the same ~56/35 candidate rows (28/28, 18/17 per arm). At the DOMAIN unit the phase mandates elsewhere, the human ratio's CI will be comparable to the automated 0.58-1.9 it is meant to break. Unlike the linking test (CONT-ENTRY 150), no power check is done for the human endpoint, so it may be built and returned still undecided.
  - _fix:_ Add a power/MDE note for the blind-rating ratio at the domain unit, and hedge "the tie-breaker" to "the one unentangled measurement, though it may itself be underpowered on this candidate set."
- **[minor]** `scripts/dcs_cont_qprobe.py (build/keep), dcs_cont_f6_validation.py, dcs_cont_f2_variants.py` (no number change)
  - The new probe scripts narrow rows by domain-split and cell completeness directly (e.g. `if r["domain"] in keep`) rather than through dcs_cont_scope.filter_rows, and pool ALL slots implicitly without stamping a scope/population/census tag -- the exact "unstated slot scope" shape (C-CONT-072/083/088/089) the guard exists to prevent. Mitigating: TEST and codeword pooling ARE separately guarded, and this mirrors the frozen F5 (DR-072) probe path, so it is consistent with established method rather than a new contrast defect.
  - _fix:_ Either route the multiposition-cache narrowing through filter_rows, or stamp an explicit scope/population/split_census tag on the qprobe/f6/f2 artifacts documenting the all-slots pooling choice.
- **[minor]** `reports/DCS_CONT_QPROBE_basket.json (train_best.rho_loo vs validation_best.rho)` (no number change)
  - For basket the TRAIN LOO winner (0.6525, rel-6 L18) and the VALIDATION transfer (0.6525, rel-6 L18) are identical to four decimals, while for button the two differ (0.5935 vs 0.645). The code computes them via independent paths (LOO Spearman vs fit-on-TRAIN/score-on-VAL Spearman), so this is most likely coincidence, but an exact 4-dp match across a train-search and a held-out transfer warrants a sanity check that validation_best is not inadvertently echoing the train value.
  - _fix:_ Re-run or add an assertion that validation_best.rho derives from transfer() on VA rows, not from the TRAIN grid; confirm the 0.6525==0.6525 is coincidental.
- **[minor]** `reports/DCS_CONT_F2_VARIANTS_basket.json (reference block)` (no number change)
  - The basket F2 file's reference block reports F5_ridge_train_loo=0.6241 and val_transfer=0.6784, which are the BUTTON F5 numbers (basket F5 is 0.6336 train / 0.6807 val per the qprobe JSON). The reference appears hard-coded/copied from button. Harmless to the verdict (v_hi_lo 0.538 is below both), but the artifact cites the wrong-codeword incumbent.
  - _fix:_ Populate the reference block per-codeword (basket F5 = 0.6336 / 0.6807).
