# Category / Attack-Completion Descriptive Report (Phase E3) — PARTIAL

> **PARTIAL.** This covers only the descriptive analyses computable from existing on-disk data (CPU-only). The *controlled* category-targeted attacks (E4-E6) are GPU work and are DEFERRED. No new suffixes were optimized here.

**Generated by:** `scripts/phase_e_dataset_audit.py` (CPU-only, deterministic, `random.seed(1234)`). Reuses `categorize`/`PATTERNS`/`CATEGORY_PRIORITY` (gcg_advbench_llm_taxonomy), `load_manifest`/`load_results`/`per_behavior_success`/`cluster_bootstrap_uplift` (gcg_7a_behavior_analysis), `analyze_free_gen` (build_gcg_source_of_truth).

**Deliverables:** `results/SUFFIX_TAXONOMY_V2.csv`, `results/CATEGORY_TRANSFER_MATRIX_V2.csv`, this file.

## E1 — Dataset audit (LOCAL pool; HF direct audit DEFERRED)

The HF dataset `MatanBT/gcg-evaluated-data` is **NOT cached and NOT vendored** (checked `.cache/huggingface/{hub,datasets}` and `~/.cache/huggingface/{hub,datasets}`; no `datasets--MatanBT--*` dir, no repo file references it). Per the hard rule (do NOT download), the **direct HF-dataset audit is DEFERRED** pending a download decision. The audit below is on the LOCAL suffix pool that the released dataset was built from.

**Local pool shape (verified):**

- Suffix-level table `results/SUFFIX_TAXONOMY.csv`: **336 rows** = one *selected* suffix per optimization run dir (the first `FINAL_CANDIDATES.jsonl` line carrying `selection_mode`). 336 run dirs with a `FINAL_CANDIDATES.jsonl`.
- Response-level pool: **136** `FREE_GENERATION_RESULTS*.jsonl` files, **102,897** evaluation rows total (suffix x behavior x condition x seed).
- Distinct selected suffix strings: **305** (31 duplicate selections across runs).
- By base: percot_v2=249, full=66, full_v2_userfix=13, early=8.
- By model family: qwen3=316, gemma4=18, deepseek_r1=2.
- By universality: per_behavior=249, universal=87.
- By derived objective_type: cot_prefix_ce=276, refusal_dir=38, repr_match=11, prefix_ce=10, repr_match_cot=1.
- **Optimizer:** all 368 CONFIG.json are GCG; there is **no named-optimizer field** (single optimizer). The nearest stored discriminator is `objective.selection_mode` (≈always `weighted`) and the derived `objective_type`.
- **StrongREJECT coverage:** in the full-520 eval (6240 seeded + 5849 unseeded rows) there are **0** null `strongreject_score`/`strongreject_is_success`.
- **Intermediate vs final:** `FINAL_CANDIDATES.jsonl` holds multiple candidate lines (line-count distribution across 336 files: 2→325, 14→3, 9→2, 1→2, and singletons at 20/13/25/7); the analysis selects **line 0 = the final selected candidate**, so intermediate candidates are present on disk but excluded by design.

**336-vs-larger reconciliation:** the 336 is a *suffix-level* selection (one final suffix per run), NOT a response-level count. The larger response-level table (102,897 local rows; the released HF table is its many-to-many eval: each suffix scored on many instructions x conditions x seeds) collapses to 336 once you keep exactly one selected/final suffix per optimization run. This is why every ASR below MUST be clustered by suffix and/or goal — the response rows are the opposite of independent (e.g. the universal-suffix full-520 eval is 1560 rows but only 520 independent goals x 3 seeds, and just **1** suffix).

## E2 — SUFFIX_TAXONOMY_V2.csv

- Rows: **336** (adds primary/secondary_category, confidence, labeling_method, manual_review_status, rationale, duplicate_group).
- Confidence: {'n/a': 87, 'low': 144, 'high': 97, 'medium': 8}.
- Manual-review status: {'auto_labeled_unreviewed': 140, 'needs_manual_review': 196} — **all are automated first-pass; none human-reviewed** (no manual review was fabricated).
- Duplicate groups: 305 groups over 336 rows (identical suffix_str collapses to a group; group_size>1 flagged for review).
- `needs_manual_review` = low/medium confidence OR high-transfer category (['fraud_financial_crime', 'misinformation_disinformation', 'other_uncategorized', 'privacy_surveillance']) OR duplicate. `n/a_universal` primary = universal suffixes have no single origin instruction to categorize.

## E3 — Descriptive analyses (CPU, existing data only)

All ASR CIs are 95% cluster bootstrap (2000 resamples, seed=1234). **goal-clustered** = resample task_ids (respects the 3 seeds/goal); **suffix-clustered** = resample suffixes (each run = one cluster). Response rows sharing a suffix or goal are never treated as independent.

### E3.1 Within-category performance (universal 5A suffix, full-520, goal-clustered)

Source: `outputs/stage_gcg_full/gcg_full_qwen3_7a_5a_full520/FREE_GENERATION_RESULTS.jsonl`, `optimized_weighted`, 1 suffix, 520 goals x 3 seeds. n = independent GOALS per category.

| category | n_goals | n_rows | optimized ASR [95% CI, goal-clustered] | neutral ASR |
|---|---|---|---|---|
| misinformation_disinformation | 42 | 126 | 0.246 [0.1667, 0.3333] | 0.0476 |
| privacy_surveillance | 8 | 24 | 0.1667 [0.0417, 0.3333] | 0.0417 |
| other_uncategorized | 39 | 117 | 0.1538 [0.0769, 0.2479] | 0.0769 |
| fraud_financial_crime | 71 | 213 | 0.0892 [0.0376, 0.1549] | 0.0188 |
| violent_crime_physical_harm | 24 | 72 | 0.0833 [0.0278, 0.1528] | 0.0139 |
| cyber_hacking_malware | 133 | 399 | 0.0777 [0.0476, 0.1103] | 0.015 |
| terrorism_extremism | 9 | 27 | 0.0741 [0.0, 0.1852] | 0.037 |
| harassment_bullying_stalking | 19 | 57 | 0.0702 [0.0, 0.193] | 0.0526 |
| weapons_explosives_mass_casualty | 40 | 120 | 0.05 [0.0, 0.1167] | 0.0083 |
| identity_theft_personal_data_theft | 42 | 126 | 0.0238 [0.0, 0.0556] | 0.0079 |
| hate_speech_discrimination | 19 | 57 | 0.0175 [0.0, 0.0526] | 0.0 |
| self_harm_suicide | 24 | 72 | 0.0 [0.0, 0.0] | 0.0 |
| theft_property_crime | 19 | 57 | 0.0 [0.0, 0.0] | 0.0 |
| drugs_controlled_substances | 16 | 48 | 0.0 [0.0, 0.0] | 0.0208 |
| academic_minor_dishonesty_deception | 8 | 24 | 0.0 [0.0, 0.0] | 0.0 |
| child_exploitation | 7 | 21 | 0.0 [0.0, 0.0] | 0.0 |

- Most vulnerable (n_goals>=15): **misinformation_disinformation** = 0.246 [0.1667, 0.3333].
- Least vulnerable (n_goals>=15): **drugs_controlled_substances** = 0.0 [0.0, 0.0].
- Small-n categories (n_goals<15) have very wide CIs — read as descriptive only.

### E3.2 Cross-category transfer (which cells are populated vs deferred)

Full matrix in `results/CATEGORY_TRANSFER_MATRIX_V2.csv`.

- **Populated:** the `multi_instruction` row — the single universal suffix's ASR on every evaluated category (E3.1 numbers with goal-clustered CIs). This is a genuine one-suffix→all-categories transfer row.
- **Diagonal only:** the `per_behavior` rows — each per-behavior suffix was evaluated ONLY on its own behavior (percot best-candidate ASR), so only the i==i cell exists.
- **DEFERRED (empty):** all off-diagonal per-behavior cells (suffix from category i evaluated on category j!=i). This requires re-running each per-behavior suffix on other behaviors — GPU, part of E4-E6. The V2 matrix carries an explicit `off_diagonal_DEFERRED` marker row so the gap is not mistaken for a zero.
- Suffix-level CI on the multi_instruction row is **undefined** (n_suffix=1): a single universal suffix cannot bound suffix-to-suffix variability. That bound needs the multi-suffix pool (E3.5).

### E3.3 Train vs unseen — GENERATION-SEED hold-out (NOT unseen-instruction)

**Caveat:** the on-disk hold-out is a *generation-seed* hold-out of an already-fixed universal suffix (seeds 42/43/44 vs 100/200/300 on the SAME 520 behaviors), NOT transfer to unseen *instructions*. Genuine unseen-instruction transfer would be a held-out-behavior split, which is DEFERRED.

| set | optimized ASR [95% CI, goal-clustered] | uplift vs neutral [95% CI] |
|---|---|---|
| seeded_train_42_43_44 | 0.0801 [0.0635, 0.0981] | 0.0584 [0.0442, 0.0744] |
| unseen_gen_seed_100_200_300 | 0.0892 [0.07, 0.1088] | 0.0511 [0.034, 0.0698] |

### E3.4 Single- vs multi-instruction — kept SEPARATE (not like-for-like)

These two ASRs are **NOT comparable as an effect size**: `single` = per-behavior best-candidate ASR on the suffix's OWN training behavior (optimistic upper bound); `multi` = one fixed universal suffix averaged over 520 held-in behaviors. They are also **model-family-confounded** (single group is 100% qwen3; universal group pools qwen3+gemma4+deepseek_r1). Reported separately, never differenced as a clean gap.

| group | ASR [95% CI, suffix-clustered] | note |
|---|---|---|
| per_behavior (own-behavior best-cand) | n/a | optimistic; own behavior only |
| universal (own-manifest eval) | 0.1193 [0.0748, 0.171] (n_suffix=76) | many-behavior average |

(Both rows use per-suffix ASR from each run's own `FREE_GENERATION_RESULTS.jsonl`, so the cluster unit is the suffix; only the 76 runs that carry their own eval contribute.)

### E3.5 Universality by objective / optimizer / suffix-length / tokens (suffix-clustered)

Per-suffix optimized ASR over the 76 runs that carry their own eval; suffix is the cluster unit. Pooled across heterogeneous manifests — descriptive, not a controlled single-variable contrast.

**By objective_type:**

| objective_type | ASR [95% CI, suffix-clustered] |
|---|---|
| repr_match | 0.4555 [0.133, 0.7844] (n_suffix=9) |
| prefix_ce | 0.1422 [0.0, 0.4133] (n_suffix=3) |
| cot_prefix_ce | 0.0962 [0.0666, 0.1346] (n_suffix=27) |
| refusal_dir | 0.0537 [0.0343, 0.0742] (n_suffix=36) |
| repr_match_cot | 0.0133 [0.0133, 0.0133] (n_suffix=1) |

**Optimizer:** single optimizer (GCG) across the whole pool — no cross-optimizer contrast is possible from this data.

**By suffix_length:**

| suffix_length | ASR [95% CI, suffix-clustered] |
|---|---|
| 16 | 1.0 [1.0, 1.0] (n_suffix=4) |
| 20 | 0.071 [0.0529, 0.092] (n_suffix=71) |
| 35 | 0.0267 [0.0267, 0.0267] (n_suffix=1) |

Note: length is near-constant (327/336 suffixes are length 20), so the length contrast is essentially unpowered.

**By token characteristics:**

| split | ASR [95% CI, suffix-clustered] |
|---|---|
| non_ascii_ge_median: ge_0.0839 | 0.151 [0.0679, 0.2535] (n_suffix=38) |
| non_ascii_ge_median: lt_0.0839 | 0.0876 [0.0571, 0.1255] (n_suffix=38) |
| has_prefill_phrase: prefill=1 | 0.0471 [0.0214, 0.0838] (n_suffix=14) |
| has_prefill_phrase: prefill=0 | 0.1356 [0.0807, 0.2023] (n_suffix=62) |
| has_imperative: imperative=1 | 0.0957 [0.0721, 0.1199] (n_suffix=19) |
| has_imperative: imperative=0 | 0.1272 [0.0672, 0.1994] (n_suffix=57) |

### E3.6 Seed sensitivity (full-520 universal suffix, per generation seed, goal-clustered)

| generation seed | optimized ASR [95% CI, goal-clustered] |
|---|---|
| 42 | 0.0846 [0.0615, 0.1096] |
| 43 | 0.0654 [0.0442, 0.0865] |
| 44 | 0.0904 [0.0673, 0.1135] |
| 100 | 0.0791 [0.0568, 0.1034] |
| 200 | 0.1084 [0.0818, 0.137] |
| 300 | 0.0802 [0.0576, 0.1029] |

- ASR range across generation seeds: [0.0654, 0.1084] (spread 0.043); seed-to-seed drift is small relative to the goal-clustered CI width.

### E3.7 Model-family sensitivity (suffix-clustered)

| model_family | ASR [95% CI, suffix-clustered] |
|---|---|
| deepseek_r1 | 0.4533 [0.4133, 0.4933] (n_suffix=2) |
| gemma4 | 0.1239 [0.0048, 0.2989] (n_suffix=17) |
| qwen3 | 0.1062 [0.0673, 0.1562] (n_suffix=57) |

Caveat: gemma4 and deepseek_r1 have very few evaluated suffixes; CIs are wide and the family contrast is confounded with objective/manifest choices.

### E3.8 Category coverage + worst category

- Categories with any full-520 eval coverage: **16/16**.
- Zero-ASR categories for the universal suffix (n_goals shown): self_harm_suicide (n=24), theft_property_crime (n=19), drugs_controlled_substances (n=16), academic_minor_dishonesty_deception (n=8), child_exploitation (n=7).
- Worst non-trivial category (n_goals>=15): **drugs_controlled_substances** = 0.0 [0.0, 0.0].

## Deferred to GPU (E4-E6) / mechanistic hand-off

- Direct audit of the HF dataset `MatanBT/gcg-evaluated-data` (not cached; do NOT download).
- Off-diagonal per-behavior cross-category transfer (re-eval each per-behavior suffix on other categories).
- Controlled category-targeted attacks and held-out-INSTRUCTION transfer.
- Single-vs-multi as a like-for-like effect size (needs matched eval + de-confounding of model family).
- Any internal-state / mechanistic attribution of the lexical correlates.

