# NEXT3 — Execute the deferred high-value levers (new techniques)

Executes the four items deferred in `HANDOFF.md` NEXT2 addendum. Env: L40S n-801..805,t-806; `poc_stage2`; forced_choice. Findings → `NEXT3_FINDINGS.md`. Isolated files to allow parallel build.

## Items
- **T1 — Cross-architecture via thinking-aware readout.** Thread `--enable-thinking {true,false,default}` through `31_validate_readouts.py`, `32_extract_pair_reps.py`, `34_intervention_sweep.py` (pass to `ds_common.apply_template(..., enable_thinking=...)`; default None → Llama unaffected). Add `ENABLE_THINKING` env to `ds_rebuild_transplant.slurm`. Then run Qwen3-14B and DeepSeek-R1-Distill-Llama-8B (both cached) transplant chains with `ENABLE_THINKING=false`. Success = gate passes → per-model IE_state/DE_context (no pooling). Biggest lever (lifts the primary claim off single-model Llama).
- **T2 — Forced-choice patchscope** (`46_forced_choice_patchscope.py`, standalone). The free-next-token patchscope failed the bomb positive control (N3) partly because the model won't emit "bomb" (safety), not because the rep lacks it. Build a patchscope that injects a rep at the probe position of a DEMO-FREE forced_choice prompt and reads the binary concept-vs-codeword choice. Positive control (layer-scanned): a clean DIRECT bomb rep must choose concept (gate). If it passes: (a) does the DS query codeword rep decode as concept (tests the paper's Patchscopes claim); (b) does a transplanted h_DS-in-Neutral rep fail to (confound-free IE_state=0 cross-check). If the PC still fails, document honestly (safety-suppression is fundamental for this readout).
- **T3 — Representational TOCTOU** (`47_repr_toctou.py`, standalone). Forward-only test of whether the depth-gated refusal CHECK generalizes across pairs without behavioral noise. On a NEUTRAL prompt, add `d_Direct` over early/mid/late windows; measure the projection of the activation onto `v_refusal` (L18) at the answer/codeword position. Compare early vs late (+ no-install baseline + norm-matched-random control). Across bomb/grenade/chlorine. If early > late across pairs, the depth-gated check generalizes even though the behavioral consequence (S4 #6) is pair-dependent.
- **T4 — Attribution-patching map** (`48_attribution_patching.py`, standalone, NEW technique). Approximate the per-(layer,position) contribution to DE_context via one backward pass (AtP: grad(metric) · (clean−corrupt) activation), giving a layer×context-position map of where the DS context installs the reading. VALIDATE top-k cells against true LayerPatch (activation patching) — report AtP only where it agrees. Correctness-gated.

## Execution
Parallel build (per-item implement→review pipeline). I verify (compile + CPU tests + tokenizer checks) before any GPU. Then run: T1 (Qwen3 ∥ DeepSeek), T2 (bomb), T3 (3 pairs), T4 (bomb). Document each in `NEXT3_FINDINGS.md`; update `PAPER_CONTRIBUTION.md`/`HANDOFF.md`. Job-isolate captures (B5); ≤6 concurrent.
